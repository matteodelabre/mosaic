[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mosaic"
version = "0.0.0"
authors = [
    { name="Mattéo Delabre", email="git.matteo@delab.re" },
]
description = "Create diagrams to visualize cost landscapes of Pareto-optimal vectors"
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Operating System :: OS Independent",
]
dependencies = [
    "numpy",
    "scipy",
    "pypoman",
]

[project.urls]
"Homepage" = "https://github.com/matteodelabre/mosaic"
"Bug Tracker" = "https://github.com/matteodelabre/mosaic/issues"

[tool.hatch.build]
sources = ["src"]
only-include = ["src"]

[tool.hatch.envs.dev]
dependencies = [
    "pytest",
    "black",
    "ruff",
]

[tool.hatch.envs.dev.scripts]
test = "pytest --exitfirst"
format = "black --diff --check --color ."
format-fix = "black ."
lint = "ruff check ."
lint-fix = "ruff check --fix ."
check-all = """
hatch run dev:lint \
&& hatch run dev:format \
&& hatch run dev:test"""

[tool.ruff.lint]
select = [
    # pydocstyle
    "D",
    # pycodestyle
    "E",
    # eradicate
    "ERA",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-fixme
    "FIX",
    # flake8-debugger
    "T10",
    # flake8-simplify
    "SIM",
    # isort
    "I",
]
ignore = ["D100", "D105", "D203", "D212"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["D104"]
"tests/*" = ["D"]
